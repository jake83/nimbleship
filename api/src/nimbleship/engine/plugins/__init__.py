"""Built-in named plugins. Each module registers its plugins on import;
this package is what nimbleship.engine.field_plugins loads to populate
the registry."""

from nimbleship.engine.plugins import number_range as number_range
